/**
 * Cross-origin compatibility entry point. This preserves the 0.3.34 editor
 * surface while keeping each editor inside a dedicated embed-sdk subframe.
 */
export * from "../../../src/components/onlyoffice-embed-sdk/compat/subframe";
